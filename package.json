{
  "name": "ima-search-app-be",
  "version": "1.0.0",
  "description": "IMA Material Management System - CAP backend with PostgreSQL",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^9",
    "@sap/cds-pg": "^1",
    "@sap/xssec": "^4",
    "@sap/audit-logging": "^5",
    "cors": "^2.8.5",
    "express": "^4",
    "passport": "^0.7.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.13.0",
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build",
    "deploy": "cds deploy --to postgres",
    "cf-deploy": "cf push"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "postgres",
        "[development]": {
          "kind": "sqlite",
          "credentials": {
            "url": ":memory:"
          }
        }
      },
      "auth": {
        "kind": "xsuaa"
      }
    }
  }
}
