---
applications:
- name: ima-app
  random-route: true
  path: .
  memory: 512M
  buildpacks:
    - nodejs_buildpack
  env:
    NODE_ENV: production
  services:
    - ima-app-db
    - ima-app-xsuaa
  command: npm start
